// Inset so the ring stays inside its own control — the watchlist split button
// puts two focusable halves inside one border, where an outset ring would spill
// over the divider and over its neighbour.
export const FOCUS_RING =
  "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-inset focus-visible:ring-accent";
