/**
 * Session-storage flag set just before sign-out on account deletion, so the
 * post-logout landing page can show a one-time confirmation toast.
 */
export const ACCOUNT_DELETED_TOAST_KEY = "cineroll_account_deleted_toast";
