import { PolicySection } from "@/features/legal/components/policy-section";

export function PublicContentSection() {
  return (
    <PolicySection title="Public Content">
      <p>
        Film comments are public by default and may show your display name, avatar, date, and comment
        body. Comments may be hidden or removed if needed for moderation, abuse prevention, or legal
        reasons.
      </p>
    </PolicySection>
  );
}
