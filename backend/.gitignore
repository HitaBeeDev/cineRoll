node_modules
# Keep environment variables out of version control
.env

/src/generated/prisma

# Data pipeline working files (excels, master.json, exports, recalls)
/data/

# Recommender offline-eval results (generated per run)
/eval-results/
